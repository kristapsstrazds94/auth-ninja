---
description: Start or verify the Auth-Ninja demo stack
---

1. Check `docs/TASKS.md` — demo tasks 6.1+ must be `done` for a full demo. If not, report which tasks remain and offer to run `/task 6.1` first.
2. If demo exists, print commands to start it (from `demos/README.md` or `auth-ninja demo` once 6.5 is done).
3. Print manual test checklist: register, login, wrong password lockout, logout, session expiry.

Do not implement demo code unless the user asks to continue a demo task.
